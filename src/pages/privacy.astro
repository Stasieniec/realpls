---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<Layout title="privacy — real pls" description="Your privacy matters. Learn how real pls handles your data (spoiler: it doesn't).">
  <Header />
  
  <main class="main container container-narrow">
    <article class="content">
      <header class="content-header">
        <h1>privacy</h1>
        <p class="lead">
          your images stay on your device. always.
        </p>
      </header>
      
      <section class="section">
        <h2>the short version</h2>
        <div class="highlight-box">
          <ul>
            <li>images processed 100% locally in your browser</li>
            <li>we never upload, store, or transmit your images</li>
            <li>no cookies or tracking scripts</li>
            <li>no personal information collected</li>
            <li>no backend server that sees your data</li>
          </ul>
        </div>
      </section>
      
      <section class="section">
        <h2>how it works</h2>
        <p>
          when you upload an image to real pls, the file is read directly by your web browser using the 
          <a href="https://developer.mozilla.org/en-US/docs/Web/API/FileReader" target="_blank" rel="noopener noreferrer">FileReader API</a>. 
          the image data is then processed using the 
          <a href="https://developer.mozilla.org/en-US/docs/Web/API/Canvas_API" target="_blank" rel="noopener noreferrer">Canvas API</a> 
          to perform forensic analysis.
        </p>
        <p>
          all computation happens on your device. the image data never leaves your browser, never touches 
          our servers, and is discarded from memory when you close the page or analyze a new image.
        </p>
      </section>
      
      <section class="section">
        <h2>what we don't collect</h2>
        <ul class="list">
          <li>images — we never see or have access to the images you analyze</li>
          <li>personal data — no names, emails, or identification</li>
          <li>usage data — we don't track which features you use</li>
          <li>device information — we don't fingerprint your browser</li>
        </ul>
      </section>
      
      <section class="section">
        <h2>static hosting</h2>
        <p>
          this website is hosted as a static site on cloudflare pages. while cloudflare may collect 
          standard web server logs as part of their service, this is outside our control and governed by 
          <a href="https://www.cloudflare.com/privacypolicy/" target="_blank" rel="noopener noreferrer">cloudflare's privacy policy</a>.
        </p>
      </section>
      
      <section class="section">
        <h2>downloaded reports</h2>
        <p>
          the "download json" feature generates a report file locally in your browser. this file is 
          created client-side and downloaded directly to your device. no data is sent to any server.
        </p>
      </section>
      
      <section class="section">
        <h2>open source</h2>
        <p>
          don't take our word for it — the code is open for inspection. you can verify exactly what 
          the application does. there are no hidden network requests, no telemetry, and no data collection.
        </p>
      </section>
      
      <section class="section">
        <h2>contact</h2>
        <p>
          questions? reach out at 
          <a href="https://swasilewski.com" target="_blank" rel="noopener noreferrer">swasilewski.com</a>.
        </p>
      </section>
      
      <div class="update-notice">
        <p>last updated: december 2024</p>
      </div>
      
      <div class="back-link">
        <a href="/">← back to analyzer</a>
      </div>
    </article>
  </main>
  
  <Footer />
</Layout>

<style>
  .main {
    flex: 1;
    padding-top: var(--space-16);
    padding-bottom: var(--space-10);
  }
  
  .content-header {
    margin-bottom: var(--space-12);
    padding-bottom: var(--space-8);
    border-bottom: 1px solid var(--color-border);
  }
  
  .content-header h1 {
    font-size: var(--text-3xl);
    margin-bottom: var(--space-4);
  }
  
  .lead {
    font-size: var(--text-lg);
    color: var(--color-text-secondary);
    line-height: var(--leading-relaxed);
  }
  
  .section {
    margin-bottom: var(--space-8);
  }
  
  .section h2 {
    font-size: var(--text-lg);
    margin-bottom: var(--space-4);
    padding-bottom: var(--space-2);
    border-bottom: 1px solid var(--color-border);
  }
  
  .section p {
    margin-bottom: var(--space-4);
    line-height: var(--leading-relaxed);
    color: var(--color-text-secondary);
  }
  
  .highlight-box {
    padding: var(--space-4);
    border: 1px solid var(--color-ok);
    background: var(--color-ok-bg);
  }
  
  .highlight-box ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  
  .highlight-box li {
    padding: var(--space-1) 0;
    color: var(--color-text-secondary);
  }
  
  .highlight-box li::before {
    content: '✓ ';
    color: var(--color-ok);
  }
  
  .list {
    list-style: none;
    padding: 0;
  }
  
  .list li {
    position: relative;
    padding-left: var(--space-5);
    margin-bottom: var(--space-3);
    line-height: var(--leading-relaxed);
    color: var(--color-text-secondary);
  }
  
  .list li::before {
    content: '·';
    position: absolute;
    left: 0;
    color: var(--color-text-muted);
  }
  
  .update-notice {
    margin-top: var(--space-8);
    padding-top: var(--space-4);
    border-top: 1px solid var(--color-border);
  }
  
  .update-notice p {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    margin: 0;
  }
  
  .back-link {
    margin-top: var(--space-6);
  }
  
  .back-link a {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    border: none;
  }
  
  .back-link a:hover {
    color: var(--color-text);
  }
</style>
